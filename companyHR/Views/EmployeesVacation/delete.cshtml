@model company.BL.Model.EmployeesVacationVM
@inject company.BL.Interface.IEmployeeRep employeeRep
@inject company.BL.Interface.IVacationRep vactionRep


@{
    ViewBag.Title = "Delete Employee Vacation";
}

<div class="row clearfix">
    <div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
        <div class="card">
            <div class="header">
                <h2>Delete Employee Vacation</h2>

            </div>
            <div class="body">
                <form id="form_validation" asp-asp-controller="EmployeesVacation" asp-action="Delete" method="post">

                    <div class="row">

                        <div class="form-group form-float">
                            <select asp-for="employeeId" class="form-control show-tick" disabled>
                                <option value="">-- Please select Employee  --</option>
                                @{
                                    foreach (var item in employeeRep.GetAll())
                                    {
                                        <option value="@item.id">@item.employeeName</option>
                                    }
                                }
                            </select>

                        </div>

                        <div class="form-group form-float">
                            <div class="form-group form-float">
                                <select asp-for="vacationId" class="form-control show-tick" disabled>
                                    <option value="">-- Please select Bonus/Deduction  --</option>
                                    @{
                                        foreach (var item in vactionRep.GetAll())
                                        {

                                            <option value="@item.id">@item.vacationName  </option>


                                        }
                                    }
                                </select>

                            </div>

                            <div class="form-group form-float">
                                <div class="form-line">
                                    <input type="text" class="form-control" onfocus="(this.type='date')" onblur="if(!this.value)this.type='text'" asp-for="fromDate" disabled />
                                    <label class="form-label"> From Date </label>

                                </div>
                            </div>
                            <div class="form-group form-float">
                                <div class="form-line">
                                    <input type="text" class="form-control" onfocus="(this.type='date')" onblur="if(!this.value)this.type='text'" asp-for="toDate" disabled />
                                    <label class="form-label"> To Date </label>

                                </div>
                            </div>
                        </div>

                    </div>

                    <button class="btn btn-primary waves-effect" type="submit">Delete</button>
                    <a asp-controller="EmployeesVacation" asp-action="index" class="btn btn-success">Back</a>


                </form>
            </div>
        </div>
    </div>
</div>

